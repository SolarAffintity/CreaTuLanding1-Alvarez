export default function ItemListContainer({ greeting }) {
  return (
    <section className="hero">
      <h1>{greeting}</h1>
      <p>
        Este es el contenedor de productos. Aquí más adelante se mostrará el catálogo.
      </p>
    </section>
  )
}

